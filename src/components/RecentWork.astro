---
import TablerIcon from './TablerIcon.astro';

interface Props {
  heading?: string;
  description?: string;
  ctaLabel?: string;
  ctaHref?: string;
  secondaryLabel?: string;
  secondaryHref?: string;
}

const {
  heading        = 'Nos réalisations. À Montpellier et partout en France.',
  description    = "Chaque déménagement reçoit le même soin : protection systématique du mobilier, chargement méthodique et équipe ponctuelle. Voici un aperçu de nos interventions récentes.",
  ctaLabel       = 'Devis gratuit',
  ctaHref        = '/devis',
  secondaryLabel = 'Voir nos réalisations',
  secondaryHref  = '/galerie',
} = Astro.props;

const photos = [
  { src: '/images/residentiel/residentiel-01.webp', width: 1200, height: 1600, alt: "Déménagement appartement à Montpellier — chargement du mobilier par l'équipe AC Coursing" },
  { src: '/images/professionnel/professionnel-01.webp', width: 1600, height: 1200, alt: 'Déménagement de bureaux professionnels à Montpellier — AC Coursing' },
  { src: '/images/residentiel/residentiel-04.webp', width: 1600, height: 1200, alt: 'Installation et remise en place du mobilier dans un appartement — AC Coursing Montpellier' },
  { src: '/images/longue-distance/longue-distance-01.webp', width: 678, height: 452, alt: 'Déménagement longue distance depuis Montpellier — camion prêt au départ AC Coursing' },
  { src: '/images/professionnel/professionnel-02.webp', width: 1536, height: 2048, alt: "Emballage et protection de matériel professionnel lors d'un transfert de locaux" },
  { src: '/images/residentiel/residentiel-03.webp', width: 1368, height: 1824, alt: 'Transport sécurisé d\'objets fragiles en camion pour un déménagement à Montpellier' },
  { src: '/images/garde-meubles/garde-meubles-02.webp', width: 1600, height: 1200, alt: 'Garde-meubles organisé pour stockage temporaire de mobilier à Montpellier' },
];
---

<section class="bg-background py-section">
  <div class="mx-auto max-w-site px-4 sm:px-6">
    <div class="grid grid-cols-1 items-center gap-10 lg:grid-cols-2 lg:gap-14">

      <!-- Content -->
      <div class="space-y-5" data-reveal>
        <h2 class="font-heading text-display leading-tight text-foreground">{heading}</h2>
        <p class="max-w-md text-lead leading-relaxed text-muted-foreground">{description}</p>
        <div class="flex flex-wrap items-center gap-3 pt-1">
          <a
            href={ctaHref}
            class="btn-premium inline-flex min-h-11 items-center justify-center gap-1.5 rounded-full bg-primary px-5 py-2.5 text-sm font-semibold text-primary-foreground transition-opacity hover:opacity-90 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary"
          >
            {ctaLabel}
          </a>
          <a
            href={secondaryHref}
            class="inline-flex min-h-11 items-center justify-center gap-1.5 rounded-full border border-border bg-card px-5 py-2.5 text-sm font-semibold text-foreground transition-all hover:border-primary/40 hover:text-primary"
          >
            {secondaryLabel}
            <TablerIcon name="arrow-right" class="text-base leading-none" />
          </a>
        </div>
      </div>

      <!-- Masonry photo collage -->
      <div class="columns-2 gap-3 [column-fill:balance] sm:gap-4 lg:columns-3" data-reveal style="transition-delay:120ms">

        {photos.slice(0, 1).map(({ src, alt, width, height }) => (
          <div class="mb-3 overflow-hidden rounded-photo border border-border bg-card sm:mb-4">
            <img src={src} width={width} height={height} alt={alt} loading="lazy" decoding="async" class="h-auto w-full object-cover" />
          </div>
        ))}

        <!-- Testimonial card — real customer review, embedded in the grid -->
        <div class="mb-3 flex flex-col justify-between gap-3 rounded-photo bg-dark p-5 sm:mb-4">
          <p class="text-sm leading-relaxed text-primary-fg/90">
            &ldquo;Déménagement rapide et sans stress. Le prix annoncé était le prix payé. Ça change des autres.&rdquo;
          </p>
          <p class="text-xs font-semibold text-primary-fg/50">Thomas R. &middot; Lattes</p>
        </div>

        {photos.slice(1).map(({ src, alt, width, height }) => (
          <div class="mb-3 overflow-hidden rounded-photo border border-border bg-card sm:mb-4">
            <img src={src} width={width} height={height} alt={alt} loading="lazy" decoding="async" class="h-auto w-full object-cover" />
          </div>
        ))}

      </div>

    </div>
  </div>
</section>
